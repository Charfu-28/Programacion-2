#include<stdio.h>
#include<math.h>
int primo(int);
int cercano(int);
int primo (int n)
{
	int cont=2;
	while(cont<=sqrt(n))
	{
	}
	if(n%cont==0)
	return(0);
	else
	cont++;
	return(1);
}
	

int cercano(void)
{
	int x=32767;
	int y;
	y=x;
		
	while(primo==0)
	x--;
	
}

